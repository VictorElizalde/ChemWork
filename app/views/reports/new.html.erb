<div class = "container">
	<div class = "container-forms">

<div class = "center">
<h1>Nuevo Reporte</h1>
</div>
<%= simple_form_for [@group, @assignment, @report] do |f| %>

  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibieron este reporte de ser salvado:</h2>

      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lab_phase, label: 'Fase de Laboratorio' %><br />
    <%= f.select :lab_phase, ['Pre-Lab', 'Lab', 'Pos-Lab'], prompt: 'Select Lab Phase' %>
  </div>

  <div class="field">
    <%= f.file_field :file %>
  </div>

  <div class="field">
    <%= f.label "Compartir con usuarios escribe los mails separados por commas" %><br>
    <%= f.text_area :shared_with %>
  </div>

  <div class="field">
    <%= f.label "Link de youtube" %><br>
    <%= f.text_area :url %>
  </div>

  <div class="actions">
    <%= f.button :submit, 'Crear Reporte' %>
  </div>

<% end %>


<!-- <%= link_to 'Back', group_assignment_path(@group, @assignment) %> -->
</div>
</div>

<script type="text/javascript">
  $(document).ready(function() {
      $('select').material_select();
  });
</script>
